/*
    case class Research(id: ResearchId, patient: Patient, requestDate: LocalDateTime, ResultList: List[Result])
    case class Patient(firstName: String, lastName: String, dateOfBirth: LocalDate, gender: String)
    case class Result(code: String, testName: String, resultValue: ResultValue, date: LocalDateTime, status: String, explanationText: String)
*/

// import PropTypes from 'prop-types'
